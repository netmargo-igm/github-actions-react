name: Test React App
on: push
jobs: 
    my-test:
        runs-on: ubuntu-latest
        steps: 
            - name: Get repo code
            # run: echo
              uses: actions/checkout@v4
            - name: Install dependecies
              run: npm ci
            - name: Test application
              run: npm run test


                
